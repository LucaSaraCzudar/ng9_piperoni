import { NaturalTypeAnimationPipe } from './natural-type-animation.pipe';

describe('NaturalTypeAnimationPipe', () => {
  it('create an instance', () => {
    const pipe = new NaturalTypeAnimationPipe();
    expect(pipe).toBeTruthy();
  });
});
